<!DOCTYPE html>
<html>
<head>

    <title>{{data.brand}} | Sign In</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style type="text/css">

        body {
            margin: 0;
            padding: 0;
            background: black;
        }

        .container {
            max-width: 800px;
            display: flex;
            flex-direction: column;
            align-items: center;
            font-family: roboto;
            margin: auto;

            height: 80vh;
            justify-content: center;
            min-height: 406px;
        }

        form {
            max-width: 90vw;
            width: 290px;
            padding: 30px 20px;
            border: 2px solid black;
            background: white;
            border-radius: 5px;
        }

        form > * {
            display: block;
            margin-bottom: 10px;
            padding: 5px 0px;
            width: 100%;
            font-weight: bold;
        }

        form > h1 {
            font-size: 20px;
            text-align: center;
            margin: 0px;
            margin-bottom: 19px;
        }

        form > input {
            padding: 11px 5px;
            background: white;
            border: 1px solid black;
            font-size: 16px;
            box-sizing: border-box;
        }

        form > button {
            padding: 11px 5px;
            border: 1px solid black;
            background: black;
            color: white;
            font-size: 15px;
            width: 100%;
            font-weight: bold;
            margin-top: 20px;
        }

        .d-none {
            display: none;
        }
    </style>

</head>

<body>

    <div class="container">
        

        <form class="form-sign-in" action="" method="post">

            <h1>Sign In to {{data.brand}}</h1>

            <label for="email">Email</label>
            <input name="email" type="email" value="" required=true>
            
            <label for="password">Password</label>
            <input name="password" type="password" id="password" placeholder="Write your password here" required=true>

            <button id="submitButton" type="submit">Submit</button>

        </form>

        <form class="form-sign-up d-none" action="" method="post">

            <h1>Sign In to {{data.brand}}</h1>

            <label for="email">Email</label>
            <input name="email" type="email" value="" required=true>
            
            <label for="password">Password</label>
            <input name="password" type="password" id="password" placeholder="Write your password here" required=true>

            <button id="submitButton" type="submit">Submit</button>

        </form>

        <form class="form-fp d-none" action="" method="post">

            <h1>Sign In to {{data.brand}}</h1>

            <label for="email">Email</label>
            <input name="email" type="email" value="" required=true>
            
            <label for="password">Password</label>
            <input name="password" type="password" id="password" placeholder="Write your password here" required=true>

            <button id="submitButton" type="submit">Submit</button>

        </form>

    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>

    <script>

        $('.form-sign-in').on('submit',function(e) {
            e.preventDefault();
            console.log($(this).serialize());
            $.ajax({
              type: "POST",
              url: '/{{data.brand}}/gen/data/checkSignIn/n',
              data: $(this).serialize(),
              success: (data) => {
                console.log(data);
                $('#submitButton').html(data.error || data.success);
                if (data.error) return 'Click on show collections to go home';
                window.location.href = `/duty/auth/page/dutyDashboard/${data.brand}-tasks`;
              }
            });
        });

        // label: name, type: String

    </script>

</body>

</html>
