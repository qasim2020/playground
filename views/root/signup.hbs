<html>

<head>

    <title>Sign Up</title>

    <style type="text/css">

        .container {
            max-width: 800px;
            }

        form > * {
            display: block;
            margin-bottom: 10px;
            padding: 5px;
        }

        .d-none {
            display: none;
        }
    </style>

</head>

<body>

    <nav>

        <a href="/{{data.brand}}/admin/page/showCollection/{{data.brand}}-users">show Collections</a>
    
    </nav>

    <div class="container">

        <form action="" method="post">

            <input name="modelName" type="text" value="{{data.brand}}-users" required class="d-none">
            <input name="owner" type="text" value="{{data.brand}}" required class="d-none">
            <input name="role" type="text" value="auth" required class="d-none">


            <label for="name">Name</label>
            <input name="name" type="text" value="" required=true>
            
            <label for="email">Email</label>
            <input name="email" type="email" value="" required=true>

            <label for="password">Password</label>
            <input name="password" type="password" id="password" placeholder="Write your password here" required=true>

            <button id="submitButton" type="submit">Submit</button>

        </form>

        <a href="/{{data.brand}}/admin/page/signin/n">Sign In</a>

    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>

    <script>

        $('form').on('submit',function(e) {
            e.preventDefault();
            console.log($(this).serialize());
            $.ajax({
              type: "POST",
              url: '/{{data.brand}}/gen/data/saveSequence/n',
              data: $(this).serialize(),
              success: (data) => {
                console.log(data);
                $('#submitButton').html(data.error || data.success);
                if (data.success) return 'Click on show collections to go home';
                return;
                window.location.href = '/{{data.brand}}/admin/page/showCollection/{{data.brand}}-users';
              }
            });
        });

        // label: name, type: String

    </script>

</body>

</html>
