<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title></title>
</head>
<body>

    <h3>Step 1</h3>
    <p>Enter the NoCodeAPI URL to fetch airtable data below.</p>
    <input type="text" name="airtable" id="airtable">
    <button onclick="fetchData()" id="step1">Submit</button>
    <p class="error"></p>

    <h3>Step 2</h3>
    <p>If your data matches the format and there are no conflicts — you can merge the data to your existing database!</p>
    <button onclick="mergeDataIntoDatabase()">Merge the data into database</button>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
    <script>

        let fetchData = function() {
            let airtable = $('#airtable').val();
            console.log(airtable);
            console.log('here we are going to fetch the data from airtable');

            $.ajax({
                type: "GET",
                url: airtable,
                success: (data) => {
                    if (data.hasOwnProperty('records') == false) return $('.error:eq(0)').html('Sorry couldnot pull the data');
                    // localStorage.setItem("records", JSON.stringify(data.records)); 
                    console.log(data)
                    $('#step1').data({data : data.records});
                    $('.error:eq(0)').html('data has pulled successfully');
                }
            }).fail( err => console.log(err) );
        };

        let mergeDataIntoDatabase = function() {
            let data = {
                results: $('#step1').data('data').map( val => val.fields )
            }

            console.log(data);
            $.ajax({
                type: "POST",
                url: `/{{data.brand}}/admin/data/mergeDataIntoCollection/{{data.collection}}`,
                data: data,
                success: (data) => {
                if (data.hasOwnProperty('success') == false) return $('.error:eq(1)').html('Sorry merge did not happen');
                    console.log(data);
                }
            }).fail( err => console.log(err) );
        };
    
    </script>
</body>
</html>
