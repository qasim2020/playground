<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="UTF-8">

    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;300;400;500;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/root/codemirror/css/codemirror.css">
    <link rel="stylesheet" href="/root/codemirror/css/neo.css">

    <title></title>
    <style>

    body {
        font-family: "Roboto";
        padding: 0px;
    }

    .btn.active {
        background: black;
        color: white;
        border-color: black;
    }

    .btn {
        padding: 7px 8px;
        border: 2px solid black;
        display: inline-block;
        margin-bottom: 4px;
        text-decoration: none;
        color: black;
        font-size: 15px;
        background: #f2f2f2;
        height: 36px;
        box-sizing: border-box;
    }

    nav {
        display: flex;
        width: 100%;
        box-sizing: border-box;
        justify-content: space-between;
        align-items: center;
        padding: 0 10px;
    }

    .layerOne > div {
        padding: 20px 10px;
        background: whitesmoke;
        margin: 10px 0;
    }

    i.fas {
        text-decoration: none;
        color: #F2F2F2;
    }

    tr > *:nth-child(1) {
        position: absolute;
        z-index: 10;
        background: black;
        border: 4px solid black;
        margin-left: -80px;
    }

    tr > td:nth-child(2) {
        font-size: 9px;
    }

    .icon {
        width: 25px;
        display: inline-block;
        text-align: center;
    }

    .d-none {
        display: none !important;
    }

    #hiddenFields {
        margin: 17px 0 5px 0;
    }

    #hiddenFields > span {
        background: black;
        color: white;
        padding: 3px 6px;
        margin-right: 2px;
        margin-bottom: 2px;
        font-weight: bold;
        font-size: 14px;
        display: inline-block;
    }

    form {
        display: flex;
        flex-direction: column;
        max-width: 90vw;
        margin: auto;
        width: 900px;
        padding-top: 20px;
        padding-bottom: 20px;
    }

    form > label {
        font-size: 12px;
        margin-bottom: 5px;
        font-weight: bold;
        color: black;
    }

    form > input {
        font-size: 15px;
        padding: 10px;
        border: 2px solid black;
        margin-bottom: 10px;
    }

    form > button {
        font-size: 15px;
        padding: 10px;
        color: white;
        background: black;
        font-weight: bold;
        margin-bottom: 10px;
        width: fit-content;
        border: 2px solid black;
    }

    .btn.close {
        background: indianred;
        border: 2px solid black;
        color: white;
    }

    p.heading {
        font-weight: bold;
    }

    p.note > a {
        text-decoration: none;
        color: cornflowerblue;
    }

    input {
        padding: 10px;
        border: 2px solid black;
        width: 100%;
        box-sizing: border-box;
    }

    .webEditor {
        width: auto;
        box-sizing: border-box;
        border: 2px solid black;
        overflow: hidden;
        margin-bottom: 10px;
    }

    .webEditor .e:hover {
        cursor: pointer;
    }
    
    .placeholder.img {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background: black;
        padding: 12px;
        max-width: 600px;
        width: 70%;
        border: 2px solid dimgrey;
    }

    .placeholder.img > p.error {
        padding: 0px 0 9px 0;
        color: yellow;
        font-size: 14px;
    }

    .placeholder.img > p {
        margin: 0px;
    }

    .placeholder.img > p > span {
        font-size: 12px;
        margin-bottom: 6px;
        font-weight: bold;
        color: white;
        display: block;
    }

    .placeholder.img > p > input, .placeholder.img > p > .btn {
        border: white;
    }

    .placeholder.img > p > input , 
    .placeholder.href > p > input {
        font-size: 15px;
        padding: 10px;
        border: 2px solid white;
        margin-bottom: 10px;
    }


    .placeholder.img > p > input[type="file"] {
        color: white;
    }

    .placeholder.img > p > .btn {
        background: black;
        color: white;
        border: 2px solid white;
        font-weight: bold;
        height: auto;
    }

    .placeholder.href > p {
        margin-bottom: 0px;
    }

    {{#if (matchValues data.theme "property")}}
        {{> cssSnippets/propertyTemplates}}
    {{/if}}

    </style>
</head>
<body>

    <div class="layerOne">

        <nav>
            <p>{{data.owner.brandName}}</p>
            <p> 
                <a class="btn" href="/{{data.brand}}/gen/page/landingPage/n" target="_blank">Home</a>
                <a class="btn" href="/{{data.brand}}/gen/page/destroySession/n?redirect=landingPage&redirectInput=n">Log Out</a> 
            </p>
        </nav>

        <div>
            {{#each ( split data.collections )}} 
                <a 
                   class="btn pjaxTrigger {{#if (matchToCollection ../data.input ../data.brand this)}} active {{/if}}" 
                   href="/{{../data.brand}}/admin/pjax/newDashboard/{{../data.brand}}-{{this}}" 
                   data-pjax 
                   onclick="toggleClass(this, 'active')">{{camelToWord this}}</a>
            {{/each}}
            {{#if data.modules}}
            {{#each ( split data.modules ) }} 
                <a 
                    class="btn pjaxTrigger {{#if (matchValues ../data.input this)}} active {{/if}}" 
                    href="/{{../data.brand}}/admin/pjax/newDashboard/{{this}}" 
                    data-pjax 
                    onclick="toggleClass(this, 'active')">{{camelToWord this}}</a>
            {{/each}}
            {{/if}}
        </div>

        <div class="container" id="pjax-container">
            {{#if data.dataRows}}
            {{> rootTable schema=data.schema dataRows=data.dataRows}} 
            {{/if}}
            
            {{#if data.slides}}
            {{> rootSlides slides=data.slides}}
            {{/if}}

            {{#if data.tickets}}
            {{> propertyTickets}}
            {{/if}}

            {{#if data.contracts}}
            {{> propertyPrintContract}}
            {{/if}}
        </div>
    
    </div>

    <!-- DO NOT DELETE BELOW FORM -->
    <div class="formBox d-none">

    </div>

    <div class="placeholders d-none">

        <div class="href placeholder">
            <p>
                <input type="text" placeholder="https://...." onkeyup="inputChanged(this)">
            </p>
        </div>

        <div class="img placeholder">

            <p>
                <span>ENTER IMAGE LINK</span>
                <input type="text" placeholder="https://...." onkeyup="inputChanged(this)">
                <p class="error one d-none">Image does not exist. Please enter correct URL.</p>
            </p>

            <p>
                <span>OR UPLOAD IMAGE MANUALLY</span>
                <input class="" type="file" name="image-upload" value="" accept="image/*" onchange="readURL(this);">
                <p class="error two d-none">Image does not exist. Please enter correct URL.</p>
                <!-- <button class="btn white w-100" onclick="$(this).prev().click();">Upload Image</button> -->
            </p>
        
        </div>

    </div>
    <!-- DO NOT DELETE ABOVE FORM -->

    <script src="https://kit.fontawesome.com/ae3de8bee6.js" crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <script src="/root/codemirror/js/codemirror.js"></script>
    <script src="/root/codemirror/js/xml.js"></script>
    <script src="/root/ckeditor.js"></script>
    <script src="/root/pjax.js"></script>

    {{> rootJS}}

    <!-- BELOW SHOULD BE DYNAMICALLY LOADED ONLY IF NEEDED-->

    {{> rootSlideEditorJS}}

    <!-- HERE ADD DEFENCE JS and ZAKATLISTS JS SEPARATELY -->

</body>
</html>
