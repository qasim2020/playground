<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <meta charset="utf-8">
  <title>Full Page Box</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;300;400;500;700;900&display=swap" rel="stylesheet">
  <style type="text/css">
    body {
      background-color: antiquewhite;
      margin: 0;
      padding: 0;
      display: flex;

      font-family: 'Roboto', sans-serif;
    }

    .aaa {
      background-color: #004aad;
      height: 100vh;
      border: 1 px black solid;
      box-sizing: border-box;
      width: 50vw;
      justify-content: center;
      align-items: center;
      display: flex;
    }

    .bbb {
      background-color: white;
      height: 100vh;
      border: 1 px black solid;
      width: 50vw;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .ooo {
      width: 180px;
      height: auto;
    }

    .sadaf {
      color: white;
    }

    .sana {
      height: 60vh;
      width: auto;
    }

    .bubble {
      position: fixed;
      top: 5px;
      background: yellow;
      font-weight: bold;
      border-radius: 4px;
      left: 5px;
      padding: 4px 8px;
    }
  </style>
</head>

<body>

  <div class="bubble">
    <p>{{error}}</p>
    {{#if loggedIn}}
      <p>Welcome {{username}}</p>
      <a href="/logout">Log Out</a>
    {{else}}
      <a href="/signin">Log In</a>
    {{/if}}
  </div>

  <div class="aaa">
    <div class="sadaf">
      <img class="ooo" src="/logo.png">
      <H2> Welcome to TECHSHEK</H2>
      <p> Join us to promote Tech Culture of Pakistan.</p>
      {{#if loggedIn}}
        <a href="/home/redirectToCheckout">Redirect To Checkout</a>
        <!-- <button id="">Reserve your slot for 5 USD</button> -->
      {{else}}
        <a href="/purchase/signup">Sign Up</a>
        <!-- <button id="">Reserve your slot for 5 USD</button> -->
      {{/if}}
    </div>
  </div>

  <div class="bbb">
    <div>
      <img class="sana" src="/flier.jpg">
    </div>
  </div>

</body>

</html>
